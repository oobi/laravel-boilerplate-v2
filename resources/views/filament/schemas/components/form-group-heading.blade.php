{{--
    An eyebrow heading for a group of fields inside a Filament schema (e.g. a
    permission category on the Roles screen): a tier below the section title,
    and unmistakable next to option labels. Use via
    View::make('filament.schemas.components.form-group-heading')->viewData(['heading' => ...]).
--}}
<h3 class="ui-eyebrow">{{ $heading }}</h3>
