---
paths:
  - 'lang/**'
---

# Lang

## Localization uses short array keys
Use short keys in `lang/en/{group}.php` (e.g. `admin.users`) referenced via `__('group.key')` — not JSON string/full-sentence keys.
